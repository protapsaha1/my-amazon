import React from 'react';

const ReviewOrder = ({product}) => {
    console.log(product)
    return (
        <div>
            <h1>review item</h1>
        </div>
    );
};

export default ReviewOrder;